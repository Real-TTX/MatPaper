@page
@model MatPaper.Pages.Account.AccessDeniedModel
@{
    ViewData["Title"] = "Access denied";
}

<div class="auth-card">
    <div class="auth-card__brand">MatPaper</div>
    <h1 class="auth-card__title">Access denied</h1>
    <p class="auth-card__sub">You do not have permission to view this page.</p>

    <div class="btn-row">
        <a href="/" class="btn btn--secondary">Back to home</a>
    </div>
</div>
