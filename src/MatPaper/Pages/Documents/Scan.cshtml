@page "/Documents/Scan"
@model MatPaper.Pages.Documents.ScanModel
@{
    ViewData["Breadcrumb"] = L["Documents / Add"].Value;
    ViewData["AddTab"] = "camera";
}

<partial name="_AddDocumentTabs" />

@if (Model.Messages.Count > 0)
{
    <div class="form-summary">
        <ul>
            @foreach (var message in Model.Messages)
            {
                <li>@message</li>
            }
        </ul>
    </div>
}

<form class="form" method="post" enctype="multipart/form-data">
    <div asp-validation-summary="ModelOnly" class="form-summary"></div>

    <div class="form-row">
        <label for="Title">@L["Title"]</label>
        <input class="form-control" type="text" id="Title" name="Title" value="@Model.Title" placeholder='@L["Optional"]' />
    </div>

    <div class="form-row">
        <label for="CorrespondentId">@L["Correspondent"]</label>
        <select class="form-control" id="CorrespondentId" name="CorrespondentId">
            <option value="">@L["— Auto-detect —"]</option>
            @foreach (var c in Model.Correspondents)
            {
                <option value="@c.Id" selected="@(c.Id == Model.CorrespondentId)">@c.Name</option>
            }
        </select>
    </div>

    <div class="form-row">
        <label for="DocumentTypeId">@L["Document type"]</label>
        <select class="form-control" id="DocumentTypeId" name="DocumentTypeId">
            <option value="">@L["— Auto-detect —"]</option>
            @foreach (var t in Model.DocumentTypes)
            {
                <option value="@t.Id" selected="@(t.Id == Model.DocumentTypeId)">@t.Name</option>
            }
        </select>
    </div>

    <div class="form-row">
        <label for="Images">@L["Photos"]</label>
        <input class="form-control" type="file" id="Images" name="Images"
               accept="image/*" capture="environment" multiple />
        <span class="field-error" asp-validation-for="Images"></span>
    </div>

    <mp-button-row>
        <mp-button variant="primary" type="submit">@L["Create PDF"]</mp-button>
        <mp-button variant="secondary" href="/Documents">@L["Back"]</mp-button>
    </mp-button-row>
</form>
