@page "/Documents/Upload"
@model MatPaper.Pages.Documents.UploadModel
@{
    ViewData["Breadcrumb"] = L["Documents / Add"].Value;
    ViewData["AddTab"] = "files";
}

<partial name="_AddDocumentTabs" />

@if (Model.Messages.Count > 0)
{
    <div class="form-summary">
        <ul>
            @foreach (var message in Model.Messages)
            {
                <li>@message</li>
            }
        </ul>
    </div>
}

<form class="form" id="upload-form" method="post" enctype="multipart/form-data"
      data-ajax-url="/Documents/Upload?handler=Ajax" data-inbox-url="/Inbox"
      data-t-uploading='@L["Uploading…"]' data-t-added='@L["Added"]'
      data-t-duplicate='@L["Duplicate — skipped"]' data-t-failed='@L["Failed"]'
      data-t-network-error='@L["Network error"]'
      data-t-summary-added='@L["{0} added"]' data-t-summary-duplicates='@L["{0} duplicate(s) skipped"]'
      data-t-summary-failed='@L["{0} failed"]' data-t-review-link='@L["Review them in your inbox →"]'>
    @Html.AntiForgeryToken()
    <div asp-validation-summary="ModelOnly" class="form-summary"></div>

    <div class="form-row">
        <label for="Files">@L["Files"]</label>
        <div class="uploader" id="uploader">
            <div class="uploader__dropzone" id="uploader-dropzone">
                <svg class="uploader__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
                    <path d="M7 9l5-5 5 5" />
                    <path d="M12 4v12" />
                </svg>
                <p class="uploader__hint">
                    <strong>@L["Drag & drop"]</strong> @L["files here, or"]
                    <button type="button" class="uploader__browse" id="uploader-browse">@L["choose files"]</button>
                </p>
                <p class="form-help">@L["PDF or images. Files wait in your inbox until you confirm and file them."]</p>
            </div>
            <input class="form-control uploader__input" type="file" id="Files" name="Files" multiple accept=".pdf,image/*" />
        </div>
        <span class="field-error" asp-validation-for="Files"></span>
    </div>

    <ul class="upload-list" id="upload-list" hidden></ul>

    <mp-button-row>
        <mp-button variant="primary" type="submit" id="upload-submit">@L["Upload"]</mp-button>
        <mp-button variant="secondary" href="/Documents">@L["Back"]</mp-button>
    </mp-button-row>
</form>

@section Scripts {
    <script src="~/js/upload.js" asp-append-version="true"></script>
}
